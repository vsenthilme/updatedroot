import { StylePaginationDirective } from './style-pagination.directive';

describe('StylePaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new StylePaginationDirective();
    expect(directive).toBeTruthy();
  });
});
