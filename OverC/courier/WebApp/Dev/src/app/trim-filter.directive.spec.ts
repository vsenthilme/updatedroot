import { TrimFilterDirective } from './trim-filter.directive';

describe('TrimFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimFilterDirective();
    expect(directive).toBeTruthy();
  });
});
