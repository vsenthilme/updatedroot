import { TrimDropdownFilterDirective } from './trim-dropdown-filter.directive';

describe('TrimDropdownFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimDropdownFilterDirective();
    expect(directive).toBeTruthy();
  });
});
